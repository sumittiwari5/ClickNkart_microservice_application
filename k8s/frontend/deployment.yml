apiVersion: apps/v1
kind: Deployment

metadata:
  name: frontend
  namespace: clickncart

spec:
  replicas: 1

  selector:
    matchLabels:
      app: frontend

  template:
    metadata:
      labels:
        app: frontend

    spec:
      containers:
        - name: frontend
          image: sumittiwari05/clickncart-frontend:v1

          imagePullPolicy: Always

          ports:
            - containerPort: 80