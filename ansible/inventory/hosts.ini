# Fill in real IPs/DNS after `terraform apply`. In a real pipeline this
# would be generated dynamically from Terraform output (see
# docs/PHASE_2_ANSIBLE.md for the aws_ec2 dynamic inventory plugin setup)
# rather than hand-edited - static here so it's easy to read while learning.

[jenkins]
jenkins-server ansible_host=REPLACE_WITH_JENKINS_NODE_IP

[jenkins:vars]
ansible_ssh_private_key_file=~/.ssh/clickncart-key.pem