[defaults]
inventory = inventory.ini
host_key_checking = False
remote_user = ubuntu
interpreter_python = auto_silent

# Now you can simply run:
# "ansible jumpserver -m ping"
# instead of:
# "ansible -i inventory.ini jumpserver -m ping"