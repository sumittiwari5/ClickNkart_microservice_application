---
# For any additional machine (e.g. your own laptop, or a bastion host)
# that needs kubectl/helm/awscli but NOT the full Jenkins install.
- hosts: all
  become: yes
  roles:
    - kubectl
    - helm
    - awscli
    