{{ include "frontend.versionedPDB" (dict "version" .Values.liveVersion) }}

{{- if .Values.candidateVersion }}
---
{{ include "frontend.versionedPDB" (dict "version" .Values.candidateVersion) }}
{{- end }}

{{- range .Values.retiredVersions }}
---
{{ include "frontend.versionedPDB" (dict "version" .version) }}
{{- end }}
