{{ include "frontend.versionedHPA" (dict "root" $ "version" .Values.liveVersion) }}

{{- if .Values.candidateVersion }}
---
{{ include "frontend.versionedHPA" (dict "root" $ "version" .Values.candidateVersion) }}
{{- end }}

{{- range .Values.retiredVersions }}
---
{{ include "frontend.versionedHPA" (dict "root" $ "version" .version) }}
{{- end }}
