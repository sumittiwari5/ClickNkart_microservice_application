{{ include "order-service.versionedHPA" (dict "root" $ "version" .Values.liveVersion) }}

{{- if .Values.candidateVersion }}
---
{{ include "order-service.versionedHPA" (dict "root" $ "version" .Values.candidateVersion) }}
{{- end }}

{{- range .Values.retiredVersions }}
---
{{ include "order-service.versionedHPA" (dict "root" $ "version" .version) }}
{{- end }}
