{{ include "order-service.versionedPDB" (dict "version" .Values.liveVersion) }}

{{- if .Values.candidateVersion }}
---
{{ include "order-service.versionedPDB" (dict "version" .Values.candidateVersion) }}
{{- end }}

{{- range .Values.retiredVersions }}
---
{{ include "order-service.versionedPDB" (dict "version" .version) }}
{{- end }}
