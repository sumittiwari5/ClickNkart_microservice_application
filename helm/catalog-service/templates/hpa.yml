{{ include "catalog-service.versionedHPA" (dict "root" $ "version" .Values.liveVersion) }}

{{- if .Values.candidateVersion }}
---
{{ include "catalog-service.versionedHPA" (dict "root" $ "version" .Values.candidateVersion) }}
{{- end }}

{{- range .Values.retiredVersions }}
---
{{ include "catalog-service.versionedHPA" (dict "root" $ "version" .version) }}
{{- end }}
