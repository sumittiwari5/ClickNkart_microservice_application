{{ include "catalog-service.versionedPDB" (dict "version" .Values.liveVersion) }}

{{- if .Values.candidateVersion }}
---
{{ include "catalog-service.versionedPDB" (dict "version" .Values.candidateVersion) }}
{{- end }}

{{- range .Values.retiredVersions }}
---
{{ include "catalog-service.versionedPDB" (dict "version" .version) }}
{{- end }}
