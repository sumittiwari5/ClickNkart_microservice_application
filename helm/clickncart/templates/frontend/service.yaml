apiVersion: v1
kind: Service

metadata:
  name: frontend
  namespace: {{ .Values.namespace }}

spec:
  selector:
    app: frontend

  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP

  type: ClusterIP