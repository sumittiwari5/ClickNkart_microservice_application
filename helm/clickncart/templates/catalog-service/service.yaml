apiVersion: v1
kind: Service

metadata:
  name: catalog-service
  namespace: {{ .Values.namespace }}

spec:
  selector:
    app: catalog-service

  ports:
    - name: http
      port: 8082
      targetPort: 8082
      protocol: TCP

  type: ClusterIP