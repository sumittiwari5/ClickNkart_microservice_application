apiVersion: v1
kind: Service

metadata:
  name: order-service
  namespace: {{ .Values.namespace }}

spec:
  selector:
    app: order-service

  ports:
    - name: http
      port: 8083
      targetPort: 8083
      protocol: TCP

  type: ClusterIP